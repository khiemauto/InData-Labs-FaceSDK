# This is Conda environment file
# Usage: `conda env update -f environment.yml`

name: face_sdk

channels:
  - pytorch
  - conda-forge

dependencies:
  - pip
  - python 
  - pytorch::pytorch
  - pytorch::torchvision
  - pandas
  - opencv
  - git-lfs

  - pip:
    - scikit-image
    - pytest
    - pre-commit
    - black
    - flake8